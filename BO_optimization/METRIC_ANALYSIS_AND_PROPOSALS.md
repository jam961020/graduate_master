# Metric 분석 및 개선안

**Date**: 2025-11-15
**목적**: 현재 평가 Metric의 문제점 파악 및 개선안 제시

---

## 📊 현재 Metric 분석 (Current Implementation)

### 구현 위치
- `optimization.py:52-147` (`line_equation_evaluation`)

### 계산 방식

```python
# 각 선마다 (5개: longi_left, longi_right, fillet_left, fillet_right, collar_left)

# 1. 직선 방정식 변환
A, B, C = to_line_eq(x1, y1, x2, y2)  # Ax + By + C = 0 (정규화됨)

# 2. 방향 점수 (기울기 차이 기반)
slope = -A / B
slope_diff = abs(slope_gt - slope_det)
direction_sim = 1.0 / (1.0 + slope_diff)  # [0, 1]

# 3. 거리 점수 (평행 거리 기반)
parallel_dist = abs(A_det * mid_x + B_det * mid_y + C_det)
distance_threshold = diagonal * 0.05  # 640×480 → 40px
distance_sim = max(0, 1 - parallel_dist / distance_threshold)  # [0, 1]

# 4. 최종 선 점수
line_score = 0.6 * direction_sim + 0.4 * distance_sim

# 5. 전체 점수
score = mean(line_scores)  # 5개 선 평균
```

---

## 🔍 문제점 분석

### 문제 1: Distance Threshold가 너무 작음 (40px)

**현상**:
```
distance_threshold = sqrt(640^2 + 480^2) * 0.05 = 800 * 0.05 = 40px
```

**영향**:
- 40px 오차 → 0점
- 361px 오차 → 당연히 0점
- **너무 가혹한 패널티**

**실제 케이스 (Iter 9)**:
```
LONGI_RIGHT:  거리 15px → distance_sim = 1 - 15/40 = 0.625
FILLET_RIGHT: 거리 11px → distance_sim = 1 - 11/40 = 0.725
FILLET_LEFT:  거리 361px → distance_sim = 0.0 (실패)
```

### 문제 2: Direction Sim이 비선형 (가파른 하락)

**수식**:
```python
direction_sim = 1.0 / (1.0 + slope_diff)
```

**특성**:
```
slope_diff =  0.0 → 1.000
slope_diff =  0.5 → 0.667 (-33%)
slope_diff =  1.0 → 0.500 (-50%)
slope_diff =  2.0 → 0.333 (-67%)
```

**문제**:
- 기울기 차이가 조금만 커져도 급격히 하락
- 부드러운 부분 점수 부족
- BO가 최적화하기 어려운 함수 형태

### 문제 3: 1개 실패의 과도한 영향

**계산**:
```python
score = mean(line_scores)  # 단순 평균
```

**실제 케이스 (Iter 9)**:
```
5개 선 점수:
  longi_left:   0.0 (실패)
  longi_right:  0.7
  fillet_left:  0.0 (연쇄 실패)
  fillet_right: 0.8
  collar_left:  0.7

평균 = (0 + 0.7 + 0 + 0.8 + 0.7) / 5 = 0.44
```

**시각적 품질**: 3개 선이 정확 (10~15px 오차)
**Metric 점수**: 0.44 (낮음)

→ **시각적 품질과 불일치**

### 문제 4: 정규화 범위 불명확

- `direction_sim`: [0, 1] ✓
- `distance_sim`: [0, 1] ✓ (하지만 threshold 문제)
- 최종 score: [0, 1] ✓

범위는 맞지만 **값의 분포가 0.4~0.8에 집중**되어 변별력 부족

---

## 💡 개선안 제시

### Metric 1: Exponential Decay (부드러운 거리 패널티)

#### 설계 철학
```
"거리 오차는 연속적으로 감소해야 한다"
- 40px와 41px는 비슷한 점수를 받아야 함
- 100px 오차도 부분 점수를 받아야 함
- 점진적 감소 → BO 최적화 용이
```

#### 수식
```python
distance_sim = exp(-parallel_dist / distance_scale)
```

**특성**:
- `distance_scale = 50px`:
  - 10px → 0.82점
  - 40px → 0.45점
  - 100px → 0.14점
  - **엄격**

- `distance_scale = 100px`:
  - 10px → 0.90점
  - 40px → 0.67점
  - 100px → 0.37점
  - **중간**

- `distance_scale = 200px`:
  - 10px → 0.95점
  - 40px → 0.82점
  - 100px → 0.61점
  - **관대**

#### 장점
1. **부드러운 gradient** → BO 최적화 쉬움
2. **부분 점수** → 변별력 향상
3. **Tunable** → scale 파라미터로 조정 가능

#### 단점
1. Scale 선택이 주관적
2. 너무 관대하면 변별력 감소

---

### Metric 2: Angle-based (각도 차이)

#### 설계 철학
```
"방향 유사도는 각도로 측정하는 것이 직관적"
- 코사인 유사도 (dot product)
- 기울기보다 해석 가능
- 수직선/수평선 처리가 자연스러움
```

#### 수식
```python
# 정규화된 방향 벡터
A_gt, B_gt = normal_vector_gt
A_det, B_det = normal_vector_det

# 코사인 유사도 (이미 정규화됨)
cos_theta = abs(A_gt * A_det + B_gt * B_det)
direction_sim = cos_theta  # [0, 1]
```

**특성**:
```
각도 차이 = 0°   → cos(0°) = 1.00
각도 차이 = 15°  → cos(15°) = 0.97
각도 차이 = 30°  → cos(30°) = 0.87
각도 차이 = 45°  → cos(45°) = 0.71
각도 차이 = 90°  → cos(90°) = 0.00
```

#### 장점
1. **기하학적으로 명확** → 해석 가능
2. **수직선 처리 안정적** (slope = inf 문제 없음)
3. **부드러운 함수** → BO friendly

#### 단점
1. 작은 각도 차이에 덜 민감 (cos는 0° 근처에서 평평)
2. 큰 각도 차이(>45°)는 이미 실패로 봐야 함

---

### Metric 3: Endpoint Distance (끝점 거리)

#### 설계 철학
```
"가장 직관적 - 끝점이 얼마나 가까운가?"
- 선 방정식 불필요
- 단순 Euclidean 거리
- 사람의 판단과 일치
```

#### 수식
```python
# 끝점 2개의 거리
dist1 = sqrt((gt_x1 - det_x1)^2 + (gt_y1 - det_y1)^2)
dist2 = sqrt((gt_x2 - det_x2)^2 + (gt_y2 - det_y2)^2)
avg_dist = (dist1 + dist2) / 2

# Exponential decay
line_score = exp(-avg_dist / distance_scale)
```

**특성** (scale=50px):
```
평균 거리 = 10px → 0.82점
평균 거리 = 25px → 0.61점
평균 거리 = 50px → 0.37점
평균 거리 = 100px → 0.14점
```

#### 장점
1. **가장 직관적** → 해석 용이
2. **선 방정식 불필요** → 계산 간단
3. **픽셀 단위 정확도** 직접 측정

#### 단점
1. **방향 정보 무시** → 평행하지만 멀리 있는 선도 높은 점수
2. **길이 변화에 민감** → 끝점이 조금만 달라도 큰 차이
3. **Scale dependent** → 이미지 크기에 따라 조정 필요

---

### Metric 4: Weighted (선별 중요도)

#### 설계 철학
```
"모든 선이 동등하게 중요하지 않다"
- Longi (세로선): 구조적 핵심 → 가중치 높게
- Collar (세로선): 중요 → 가중치 중간
- Fillet (가로선): 상대적으로 덜 중요 → 가중치 낮게
```

#### 가중치 설정
```python
weights = {
    'longi_left':   1.5,  # 중요!
    'longi_right':  1.5,  # 중요!
    'collar_left':  1.2,  # 중간
    'fillet_left':  0.8,  # 덜 중요
    'fillet_right': 0.8,  # 덜 중요
}

score = weighted_average(line_scores, weights)
```

**효과**:
```
케이스 (Iter 9):
  longi_left:   0.0 × 1.5 = 0.0
  longi_right:  0.7 × 1.5 = 1.05
  fillet_left:  0.0 × 0.8 = 0.0
  fillet_right: 0.8 × 0.8 = 0.64
  collar_left:  0.7 × 1.2 = 0.84

가중 평균 = (0 + 1.05 + 0 + 0.64 + 0.84) / (1.5+1.5+0.8+0.8+1.2)
         = 2.53 / 5.8 = 0.44

→ 큰 차이 없음 (longi_left 실패가 여전히 큼)
```

#### 장점
1. **도메인 지식 반영** → 의미 있는 평가
2. **중요한 선 강조** → 핵심 성능 집중

#### 단점
1. **가중치 선택 주관적**
2. **1개 중요 선 실패 시 여전히 큰 영향**

---

## 🎯 종합 비교

| Metric | 직관성 | 부드러움 | 변별력 | BO 친화성 | 복잡도 |
|--------|--------|----------|--------|-----------|--------|
| **Current** | 중 | ✗ 낮음 | 중 | ✗ 나쁨 | 중 |
| **Exponential** | 중 | ✓ 높음 | 높음 | ✓ 좋음 | 중 |
| **Angle** | 높음 | ✓ 높음 | 중 | ✓ 좋음 | 낮음 |
| **Endpoint** | ✓ 매우 높음 | ✓ 높음 | 높음 | ✓ 좋음 | ✓ 낮음 |
| **Weighted** | 중 | 중 | 중 | 중 | 높음 |

---

## 💡 추천 조합

### 추천 1: Angle + Exponential (균형)

```python
def metric_angle_exponential(detected, gt,
                             angle_weight=0.6,
                             dist_weight=0.4,
                             dist_scale=100.0):
    """
    방향: 각도 (코사인 유사도)
    거리: Exponential decay

    - 해석 가능
    - 부드러운 gradient
    - BO 최적화 용이
    """
    for each_line:
        # 방향
        cos_theta = abs(A_gt*A_det + B_gt*B_det)
        direction_sim = cos_theta

        # 거리
        parallel_dist = abs(A_det*mid_x + B_det*mid_y + C_det)
        distance_sim = exp(-parallel_dist / dist_scale)

        # 종합
        line_score = angle_weight * direction_sim + dist_weight * distance_sim

    return mean(line_scores)
```

**장점**:
- 각도: 직관적, 안정적
- 거리: 부드러운 패널티
- 두 가지 모두 BO friendly
- 하이퍼파라미터: `dist_scale`, `angle_weight/dist_weight`

**단점**:
- 여전히 1개 실패의 영향 큼

---

### 추천 2: Endpoint (가장 단순)

```python
def metric_endpoint_simple(detected, gt, dist_scale=50.0):
    """
    끝점 거리만 사용

    - 가장 직관적
    - 계산 간단
    - 해석 용이
    """
    for each_line:
        dist1 = euclidean(gt_p1, det_p1)
        dist2 = euclidean(gt_p2, det_p2)
        avg_dist = (dist1 + dist2) / 2

        line_score = exp(-avg_dist / dist_scale)

    return mean(line_scores)
```

**장점**:
- 가장 단순
- 사람의 직관과 일치
- 계산 빠름

**단점**:
- 방향 정보 없음 (평행하지 않아도 점수 높을 수 있음)

**해결책**:
- 각도 차이가 15° 이상이면 패널티 추가
- `if angle_diff > 15°: line_score *= 0.5`

---

### 추천 3: Hybrid (가장 강력, 복잡함)

```python
def metric_hybrid(detected, gt,
                 angle_scale=30.0,  # degrees
                 dist_scale=50.0):   # pixels
    """
    방향 + 거리 + 끝점 모두 사용

    - 가장 포괄적
    - 각 측면 반영
    - 하지만 복잡함
    """
    for each_line:
        # 방향 (각도)
        cos_theta = abs(A_gt*A_det + B_gt*B_det)
        angle_diff = arccos(cos_theta) * 180 / pi
        angle_score = exp(-angle_diff / angle_scale)

        # 거리 (평행 거리)
        parallel_dist = abs(A_det*mid_x + B_det*mid_y + C_det)
        parallel_score = exp(-parallel_dist / dist_scale)

        # 끝점 (Euclidean)
        endpoint_dist = mean([
            euclidean(gt_p1, det_p1),
            euclidean(gt_p2, det_p2)
        ])
        endpoint_score = exp(-endpoint_dist / dist_scale)

        # 종합 (가중 평균)
        line_score = (angle_score + parallel_score + endpoint_score) / 3

    return mean(line_scores)
```

**장점**:
- 가장 포괄적
- 여러 측면 반영
- Robust

**단점**:
- 복잡함
- 하이퍼파라미터 많음
- 해석 어려움

---

## 🔬 실험 계획

### Phase 1: Metric 결정

**방법**:
1. 기존 결과 몇 개(best, median, worst)로 재실행
2. 각 metric으로 점수 계산
3. 시각적 품질과 비교
4. **가장 일치하는 metric 선택**

**예상 결과**:
- Current: 0.44 (시각적으로 좋은데 점수 낮음)
- Exponential(100): 0.62 (더 관대)
- Angle: 0.68 (방향 중시)
- Endpoint(50): 0.71 (가장 직관적)

### Phase 2: 하이퍼파라미터 튜닝

선택된 metric의 scale 파라미터 조정:
- `distance_scale`: [30, 50, 100, 150, 200]
- 변별력 vs 관대함의 균형

### Phase 3: BO 재실험

새 metric으로 optimization.py 수정 후:
```bash
python optimization.py --iterations 50 --n_initial 15 --alpha 0.3
```

**기대 효과**:
- CVaR-Score correlation 증가 (현재 0.006 → 0.3+)
- BO 수렴 속도 향상
- 시각적 품질과 metric 일치

---

## 📊 예상 효과

### 현재 vs 개선

| 지표 | 현재 | Exponential | Endpoint |
|------|------|-------------|----------|
| **Best Score** | 0.5127 | 0.65 (+27%) | 0.71 (+38%) |
| **분포** | 좁음 (0.4~0.6) | 넓음 (0.2~0.8) | 매우 넓음 (0.1~0.9) |
| **CVaR-Score r** | 0.006 | 0.25 (?) | 0.35 (?) |
| **BO 수렴** | 느림 | 빠름 | 빠름 |
| **해석** | 중 | 중 | 높음 |

---

## ✅ 권장사항

### 즉시 (오늘)

1. **Endpoint Metric 구현** (가장 단순, 효과적)
2. **distance_scale = 50px로 테스트**
3. **Best/Median/Worst iteration 재평가**
4. **시각적 비교 후 결정**

### 단기 (내일)

1. 선택된 metric으로 optimization.py 수정
2. 소규모 실험 (10 iterations)
3. CVaR-Score correlation 확인

### 중기 (이번 주)

1. 전체 실험 (50 iterations)
2. Session 13과 비교
3. 논문 Figure 생성

---

## 🎓 설계 철학 요약

### 좋은 Metric의 조건

1. **시각적 품질과 일치** ← 가장 중요!
2. **연속적 (Smooth)** → BO 최적화 용이
3. **정규화** → [0, 1] 범위
4. **해석 가능** → 사람이 이해 가능
5. **변별력** → 좋음/나쁨을 명확히 구분
6. **Robust** → 극단값에 민감하지 않음

### Metric 선택 기준

```
                               복잡도
                                 ↑
                           Hybrid |
                                  |
                         Weighted |
                                  |
         Angle+Exp ──────────────o
                                  |
         Current ────────────────o
                                  |
                          Endpoint|____________→ 직관성


추천: Endpoint (단순 + 직관적 + 효과적)
대안: Angle+Exp (균형 + 이론적으로 우수)
```

---

**마지막 업데이트**: 2025-11-15
**다음 단계**: Endpoint Metric 구현 → 테스트 → 비교 → 선택
